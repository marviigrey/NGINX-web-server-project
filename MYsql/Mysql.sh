sudo apt install mysql-server
sudo mysql
#run the command below immediately you enter the sql terminal
#this automatically sets your password as "PassWord.1" you can modify it.
#ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY 'PassWord.1';
#after doing this exit the terminal and run
#     sudo mysql_secure_installation
